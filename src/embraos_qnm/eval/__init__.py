"""Eval harness for the capability-cost pre-registration (PREREG-Capability-Cost.md).

Arms 0 (no constraint) and P (prompt constraint) over a stock Core, for the **no-pretense**
constraint (honest uncertainty / no fabrication). Arm A (architecture) is Phase 2 and slots
into the same instrument. The instrument here — frozen probe sets, the judge rubric, the
decoding — is the registered artifact; results are reproducible from it (greedy decoding).
"""
